<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:Labb2OOAD.ViewModels;assembly:Labb2OOAD"
             xmlns:behaviors="clr-namespace:Labb2OOAD.Behaviors;assembly:Labb2OOAD"
             x:Class="Labb2OOAD.Views.SearchPage"
             Title="">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Entry Placeholder="Enter Cityname" HorizontalTextAlignment="Center" Text="{Binding Cityname.Value, Mode=TwoWay}" IsEnabled="{Binding Cityname.isValid}" >
                <Entry.Behaviors>  
                    <behaviors:EventToCommandBehavior  
                        EventName="TextChanged"  
                        Command="{Binding ValidateCityNameCommand}" />  
                </Entry.Behaviors> 
            </Entry>
            <Button BindingContext="SearchViewModell" Text="Search" IsEnabled="{Binding Cityname.isValid}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
